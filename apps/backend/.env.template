# Copy this file to .env and adjust values for your environment

ENCRYPTION_KEY=change_me_64_hex_chars____________________________________
ENCRYPTION_SALT=change_me_32_hex_chars________________
JWT_SECRET=change_me_secure_jwt_secret

MONGO_URI=mongodb://localhost:27017/bootcamp-chat

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=ktb

# Store Configuration
# Session store: "redis" (default, multi-server) or "mongodb" (single-server)
SESSION_STORE_TYPE=redis
# Chat data store: "redis" (default, multi-server) or "local" (single-server)
CHAT_STORE_TYPE=redis

PORT=5001
WS_PORT=5002

OPENAI_API_KEY=sk-...
